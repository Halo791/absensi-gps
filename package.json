{
  "name": "attendance-demo-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "overrides": {
    "lodash": "4.17.21"
  },
  "scripts": {
    "build": "npm run build --workspace frontend",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "seed": "npm run seed --workspace backend"
  }
}
